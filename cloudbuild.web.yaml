# Cloud Build パイプライン: web アプリを Artifact Registry にビルドして Cloud Run へデプロイ
# 利用方法:
#   gcloud builds submit --config=cloudbuild.web.yaml \
#     --substitutions=_REGION=asia-northeast1,_REPO=mirai-gikai-ota,_SERVICE=mirai-gikai-ota-web

substitutions:
  _REGION: asia-northeast1
  _REPO: mirai-gikai-ota
  _SERVICE: mirai-gikai-ota-web
  _SERVICE_ACCOUNT: mirai-gikai-ota-web@${PROJECT_ID}.iam.gserviceaccount.com

options:
  logging: CLOUD_LOGGING_ONLY
  machineType: E2_HIGHCPU_8

steps:
  - id: build
    name: gcr.io/cloud-builders/docker
    args:
      - build
      - --file=web/Dockerfile
      - --tag=${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_SERVICE}:${SHORT_SHA}
      - --tag=${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_SERVICE}:latest
      - .

  - id: push
    name: gcr.io/cloud-builders/docker
    args:
      - push
      - --all-tags
      - ${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_SERVICE}

  - id: deploy
    name: gcr.io/google.com/cloudsdktool/cloud-sdk
    entrypoint: gcloud
    args:
      - run
      - deploy
      - ${_SERVICE}
      - --image=${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_SERVICE}:${SHORT_SHA}
      - --region=${_REGION}
      - --service-account=${_SERVICE_ACCOUNT}
      - --allow-unauthenticated
      - --port=8080
      - --memory=1Gi
      - --cpu=1
      - --concurrency=80
      - --min-instances=0
      - --max-instances=10
      - --timeout=3600
      - --set-env-vars=GOOGLE_VERTEX_LOCATION=${_REGION},GOOGLE_VERTEX_PROJECT=${PROJECT_ID}
      # 機密値は Secret Manager から読み込ませる
      - --update-secrets=SUPABASE_URL=mirai-gikai-ota-supabase-url:latest,SUPABASE_SERVICE_ROLE_KEY=mirai-gikai-ota-supabase-service-key:latest,NEXT_PUBLIC_SUPABASE_URL=mirai-gikai-ota-supabase-url:latest,NEXT_PUBLIC_SUPABASE_ANON_KEY=mirai-gikai-ota-supabase-anon:latest,REVALIDATE_SECRET=mirai-gikai-ota-revalidate-secret:latest

images:
  - ${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_SERVICE}:${SHORT_SHA}
  - ${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_SERVICE}:latest
