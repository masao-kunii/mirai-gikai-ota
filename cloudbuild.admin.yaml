# Cloud Build パイプライン: admin アプリを Artifact Registry にビルドして Cloud Run へデプロイ
# 利用方法:
#   gcloud builds submit --config=cloudbuild.admin.yaml \
#     --substitutions=_REGION=asia-northeast1,_REPO=mirai-gikai-ota,_SERVICE=mirai-gikai-ota-admin

substitutions:
  _REGION: asia-northeast1
  _REPO: mirai-gikai-ota
  _SERVICE: mirai-gikai-ota-admin
  _SERVICE_ACCOUNT: mirai-gikai-ota-admin@mirai-gikai-ota.iam.gserviceaccount.com

options:
  logging: CLOUD_LOGGING_ONLY
  machineType: E2_HIGHCPU_8

availableSecrets:
  secretManager:
    - versionName: projects/${PROJECT_ID}/secrets/next-public-supabase-url/versions/latest
      env: SUPABASE_URL_VALUE
    - versionName: projects/${PROJECT_ID}/secrets/next-public-supabase-publishable-key/versions/latest
      env: NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY
    - versionName: projects/${PROJECT_ID}/secrets/supabase-secret-key/versions/latest
      env: SUPABASE_SECRET_KEY

steps:
  - id: build
    name: gcr.io/cloud-builders/docker
    entrypoint: bash
    env:
      - DOCKER_BUILDKIT=1
    secretEnv:
      - SUPABASE_URL_VALUE
      - NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY
      - SUPABASE_SECRET_KEY
    args:
      - -c
      - |
        docker build \
          --file=admin/Dockerfile \
          --build-arg=NEXT_PUBLIC_SUPABASE_URL="$$SUPABASE_URL_VALUE" \
          --build-arg=SUPABASE_URL="$$SUPABASE_URL_VALUE" \
          --build-arg=NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY="$$NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY" \
          --build-arg=SUPABASE_SECRET_KEY="$$SUPABASE_SECRET_KEY" \
          --build-arg=NEXT_PUBLIC_WEB_URL="https://mirai-gikai-ota-web-tvu5b3eo5a-an.a.run.app" \
          --tag=${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_SERVICE}:${BUILD_ID} \
          --tag=${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_SERVICE}:latest \
          .

  - id: push
    name: gcr.io/cloud-builders/docker
    args:
      - push
      - --all-tags
      - ${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_SERVICE}

  - id: deploy
    name: gcr.io/google.com/cloudsdktool/cloud-sdk
    entrypoint: gcloud
    args:
      - run
      - deploy
      - ${_SERVICE}
      - --image=${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_SERVICE}:${BUILD_ID}
      - --region=${_REGION}
      - --service-account=${_SERVICE_ACCOUNT}
      # admin は IAP / 内部からのアクセスのみを想定。外部 unauth は外す。
      - --no-allow-unauthenticated
      - --port=8080
      - --memory=1Gi
      - --cpu=1
      - --concurrency=40
      - --min-instances=0
      - --max-instances=5
      - --timeout=3600
      - --set-env-vars=GOOGLE_VERTEX_LOCATION=${_REGION},GOOGLE_VERTEX_PROJECT=${PROJECT_ID},NEXT_PUBLIC_WEB_URL=https://mirai-gikai-ota-web-tvu5b3eo5a-an.a.run.app,ADMIN_URL=https://mirai-gikai-ota-admin-tvu5b3eo5a-an.a.run.app
      - --update-secrets=SUPABASE_URL=next-public-supabase-url:latest,NEXT_PUBLIC_SUPABASE_URL=next-public-supabase-url:latest,NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=next-public-supabase-publishable-key:latest,SUPABASE_SECRET_KEY=supabase-secret-key:latest

images:
  - ${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_SERVICE}:${BUILD_ID}
  - ${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_SERVICE}:latest
