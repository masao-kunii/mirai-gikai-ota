# 別地域向け Fork 手順書

> このガイドは、川崎市議会版（みらい議会ー川崎版）の作成者が別の地方議会向けに Fork する際の手順をまとめたものです。

---

## 1. はじめに

本リポジトリはオープンソースで公開されており、他の地方議会向けにカスタマイズして利用できます。
川崎市固有の情報は `site.config.ts` に集約されているため、**最小限の変更でForkが可能**です。

---

## 2. 最低限変更が必要なファイル

### 2-1. `web/src/config/site.config.ts` ← **最重要・ここだけ変えればほぼ動く**

```typescript
export const siteConfig = {
  siteName: "みらい議会ーXX版",          // ← サイト名を変更
  siteDescription: "XX市議会で今どんな議案が検討されているか...",  // ← 説明を変更
  cityName: "XX市",                       // ← 市区町村名を変更
  councilName: "XX市議会",               // ← 議会名を変更
  keywords: ["みらい議会ーXX版", "議案", "XX市", ...],  // ← キーワードを変更
  councilBaseUrl: "https://www.city.xx.jp/",             // ← 議会サイトのベースURLを変更
  councilBillsDetailUrl: "https://www.city.xx.jp/...",   // ← 議案・議決結果一覧URLを変更
  twitterHashtag: "みらい議会XX版",      // ← Xのハッシュタグを変更（#なし）
  externalLinks: {
    report: "https://forms.gle/...",       // ← 問題報告フォームURLを変更
    aboutNote: "https://note.com/...",     // ← サービス説明noteのURLを変更
    donation: "https://team-mir.ai/support/donation",  // ← 変更不要（チームみらい共通）
    teamAbout: "https://team-mir.ai/about",            // ← 変更不要（チームみらい共通）
    terms: "https://team-mir.ai/terms",                // ← 変更不要（チームみらい共通）
    privacy: "https://team-mir.ai/privacy",            // ← 変更不要（チームみらい共通）
    faq: "https://team-mirai.notion.site/FAQ-...",     // ← 変更不要（チームみらい共通）
  },
} as const;
```

### 2-2. `admin/src/config/site.config.ts`

```typescript
export const siteConfig = {
  siteName: "みらい議会ーXX版",
  cityName: "XX市",
  councilName: "XX市議会",
  councilBaseUrl: "https://www.city.xx.jp/",
  councilBillsDetailUrl: "https://www.city.xx.jp/...",
  councilFactionExamples: "みらいXX市議会、XX市自由民主党議員団、XX市公明党議員団等",
} as const;
```

### 2-3. `web/public/manifest.json`（JSONのためTSインポート不可・手動編集）

```json
{
  "name": "みらい議会ーXX版",
  "short_name": "みらい議会",
  ...
}
```

### 2-4. `packages/seed/main/data.ts`（テストデータ）

定例会・会派・委員会のシードデータを差し替えてください。

```typescript
// 定例会の例
export const COUNCIL_SESSIONS = [
  {
    name: "令和XX年 第X回定例会",
    slug: "XXX-teireikai",
    start_date: "XXXX-XX-XX",
    end_date: "XXXX-XX-XX",
    is_active: true,
  },
];

// 会派の例
export const FACTIONS = [
  { name: "みらいXX市議会" },
  { name: "XX市自由民主党議員団" },
  ...
];
```

---

## 3. 画像素材の差し替え

| ファイル | 用途 | サイズ |
|---|---|---|
| `web/public/ogp.jpg` | OGP画像（SNSシェア時） | 1200×630px |
| `web/public/img/logo.svg` | サイトロゴ | SVG |
| `web/public/img/hero_background.png` | トップページヒーロー背景 | 任意 |
| `web/public/icons/pwa/icon_android_192.png` | PWAアイコン（Android） | 192×192px |
| `web/public/icons/pwa/icon_ios.png` | PWAアイコン（iOS） | 180×180px |

---

## 4. 環境変数の設定

`.env.example` を `.env` にコピーして値を設定してください。

```bash
cp .env.example .env
```

主要な設定項目：

| 変数名 | 説明 |
|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | SupabaseプロジェクトURL |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase匿名キー |
| `SUPABASE_SERVICE_ROLE_KEY` | Supabaseサービスロールキー |
| `NEXT_PUBLIC_WEB_URL` | 公開WebアプリのURL |
| `NEXT_PUBLIC_GA_TRACKING_ID` | Google Analytics ID（任意） |

---

## 5. Supabase・DBセットアップ手順

```bash
# ローカルSupabaseの起動
npx supabase start

# マイグレーションの適用
pnpm db:migrate

# 型定義の生成
pnpm db:types:gen

# シードデータの投入
pnpm seed
```

詳細は `docs/kawasaki/20260219_1000_ローカル環境起動手順.md` を参照してください。

---

## 6. Langfuse プロンプト設定

AIチャット・インタビュー機能では Langfuse でプロンプトを管理しています。

1. Langfuse のプロジェクトを作成し、環境変数を設定
2. 以下のプロンプトを議会・地域情報に合わせて更新：
   - AIチャット用プロンプト
   - AIインタビュー用プロンプト
3. 更新後は管理画面で動作確認

---

## 7. リリース前チェックリスト

- [ ] `web/src/config/site.config.ts` の全項目を確認・更新
- [ ] `admin/src/config/site.config.ts` の全項目を確認・更新
- [ ] `web/public/manifest.json` を更新
- [ ] OGP画像（`web/public/ogp.jpg`）を差し替え
- [ ] ロゴ・アイコン類を差し替え
- [ ] シードデータを差し替えて `pnpm seed` 実行
- [ ] Langfuse プロンプトを更新
- [ ] `pnpm typecheck` でエラーがないことを確認
- [ ] `pnpm lint` でエラーがないことを確認
- [ ] `pnpm dev` でローカル起動し、各ページの表示を確認
  - [ ] トップページ（サイト名・説明・外部リンク）
  - [ ] 議案詳細ページ（免責事項・SNSシェア）
  - [ ] Admin管理画面（タイトル・プレースホルダー）
- [ ] 各ページの OGP プレビューを確認（[OGP確認ツール](https://ogp.biz/)等）
- [ ] SNS シェアのハッシュタグを確認
- [ ] `grep -r "川崎" web/src admin/src --include="*.ts" --include="*.tsx" | grep -v site.config` で漏れがないことを確認
