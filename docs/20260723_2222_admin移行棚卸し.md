# 管理画面 新アーキ移行 棚卸し（旧 admin → apps/admin）

作成: 2026-07-23 / **最終更新: 2026-08-01** / 対象ブランチ基準: `ota/develop`

## 目的
旧 `admin/`（Next.js / Supabase Auth / service_role）から、新アーキ管理フロント
`apps/admin`（Vite + TanStack Router/Query SPA / Cloudflare Access / app_admin 経由の
Hono RPC）への移行状況を棚卸しし、**完全カットオーバーまでの残作業**と優先順位を示す。

新アーキの原則は [docs/TARGET_ARCHITECTURE.md](TARGET_ARCHITECTURE.md)（Phase 4）。
旧 admin は**カットオーバーまで並行稼働**（ビッグバン書き換え禁止）。

カットオーバーの具体手順は [20260801_0758_admin カットオーバー準備.md](20260801_0758_adminカットオーバー準備.md) を参照。

## 凡例
- ✅ 移行済み（`ota/develop` にマージ済み）
- 🟡 進行中（オープン PR）
- ⬜ 未移行
- 🚫 移行不要（廃止／別置換／インフラで対応）

---

## 1. 移行状況（機能）

### マスタ・運用ドメイン

| ドメイン | 新 admin の画面 | api（`/api/admin/*`） | 状態 |
|---|---|---|---|
| タグ | `/tags` | `tags` | ✅ #115 |
| 会派 | `/factions` | `factions` | ✅ #117 |
| 委員会 | `/committees` | `committees` | ✅ #117 |
| 議会会期（排他アクティブ化つき） | `/council-sessions` | `council-sessions` | ✅ #118 |
| 議事録（本文 Markdown 編集） | `/minutes`, `/minute-edit/:id` | `minutes` | ✅ #136 |
| 専門家（読み取り） | `/experts` | `experts` | ✅ #137 |

### 議案（bills）

| 機能 | 新 admin の画面 | 状態 |
|---|---|---|
| 基本 CRUD（審議状況・公開状態・提出区分・会期・委員会 ほか） | `/bills` | ✅ #119 |
| タグ付与（M2M） | `/bills`（行編集） | ✅ #120 |
| 会派スタンス | `/bills`（行編集） | ✅ #121 |
| 本文（難易度別 Markdown） | `/bill-contents/:id` | ✅ #122 |
| `knowledge_source` ／ チャット参照 | `/bills`（行編集） | ✅ #123 |
| 議案マージ（重複統合） | `/bills-merge` | ✅ #139 |
| **本文の AI 生成** | `/bill-contents/:id`（AIで生成） | ✅ #140 |
| **議事録からの AI 抽出＋取り込み** | `/bills-extract` | ✅ #141 |
| **公式サイトからの一括取り込み** | `/bills-import` | 🟡 PR #142 |

### インタビュー・モデレーション

| 機能 | 新 admin の画面 | 状態 |
|---|---|---|
| インタビュー設定（受付/終了の切替・基本編集） | `/interview-configs` | ✅ #126 |
| 審査キュー（モデレーション承認/却下） | `/review-queue` | ✅ #124 |
| 通報（レポート単位の集約・非公開化） | `/report-flags` | ✅ #138 |

---

## 2. 未移行（2026-08-01 の決定により切り替え前の作業はなし）

| 機能 | 旧 feature | 決定 |
|---|---|---|
| 管理者管理 | `admins` (476) | 🚫 **移行不要**。Cloudflare Access のポリシー（許可メール / Google Workspace グループ）で管理者を管理する方針に決定。**アプリ側に管理者テーブルは持たない**（現 api 実装のまま） |
| 議案サムネイル画像 | `bills-edit` の `thumbnail-upload` / `thumbnail-storage` | ⬜ **当面なしで運用**。必要になったら R2 バケット＋公開 URL 設計＋アップロード経路を設計して追加する（切り替えの前提にはしない） |
| 速報版（sokuhou）からの取込 | `ai-collection` の `import-from-sokuhou` | ⬜ 定例会ページ取込（#142）の実運用を見てから判断 |

---

## 3. 移行不要（廃止・置換・不要化）

| 機能 | 旧 feature（LOC） | 判断 |
|---|---|---|
| トピック分析 | topic-analysis (3354) | 🚫 **廃止**。旧 topic_analysis は非公開レポート混入の privacy 問題があり、新設計は事前同意＋public_reader 経由の集約（apps/web の opinions-summary）に置換済み |
| インタビューシミュレーション | interview-simulation (6010) | 🚫 dev/評価ツール。後継は `apps/api/src/eval/`（eval harness）。管理機能としての移行は不要 |
| 認証 | auth (369) | 🚫 **Cloudflare Access**（Zero Trust + Google IdP）に置換。自前ログイン/セッションは移行しない |
| インタビュー質問の事前作成 | interview-config の質問エディタ | 🚫 **不要**。新エンジンは対象ごとに設定をオンデマンド自動作成し、質問は loop で動的生成する（`interview_questions` 不使用） |
| AI 収集の実行履歴（runs） | ai-collection の `storage.ts` / `run-history` | 🚫 **不要**。旧実装はローカルファイルにドラフトを保存していたが、Workers ではファイルシステムを持てないため、抽出→レビュー→取り込みの**ステートレス2段**に置き換え（#141）、履歴という概念自体が不要になった |
| MCP ツール | mcp (530) | 要判断（内部向け。新アーキで必要かは別途） |

---

## 4. カットオーバー条件（旧 admin 停止の目安）

1. ✅ 主要運用機能の移行完了（§1 のとおり。#142 のマージで議案系も完了）
2. ✅ 未移行機能の方針決定（2026-08-01: 管理者管理は Access で代替＝移行不要／サムネイルは当面なし／旧 admin は切り替え後に停止）
3. ✅ **apps/admin のデプロイ経路を用意**（#143 で `wrangler.jsonc` ＋ `deploy` スクリプト。`--dry-run` 済み・実デプロイは未実施）
4. ⬜ admin 用サブドメインと Routes、**本番 Cloudflare Access アプリの構築**＋api への `CF_ACCESS_TEAM_DOMAIN` / `CF_ACCESS_AUD` 設定
5. ⬜ 新 admin での**受け入れ確認**（実データでの一通りの運用）
6. ⬜ 旧 admin（Cloud Run）の停止・社内リンクの切り替え

→ 残るのは **4以降のインフラ作業のみ**（コード実装の残作業なし）。

→ 手順の詳細は [カットオーバー準備](20260801_0758_adminカットオーバー準備.md)。

## 5. 補足
- 旧 admin の LOC は移行前の実測（テスト含む）。新アーキでは API 化＋SPA 化で総量が圧縮される傾向にある（例: 議案系の複数 feature が `apps/api/src/routes/admin/bills*.ts` ＋ `apps/admin/src/features/bills*/` に集約）。
- 一括取り込み（#142）では旧実装の純粋関数（HTML パーサ・マッピング・本文組み立て）を**テスト57件ごと** `packages/shared/src/bills-import/` へ移植した。パーサは正規表現のみで外部ライブラリ非依存のため Workers でもそのまま動作する。
